name = "ai-news-cron"
main = "workers/index.js"
compatibility_date = "2024-01-01"

# Cron 触发器配置
[triggers]
crons = ["0 1 * * *"]  # 每天 UTC 01:00 (北京时间 09:00)

# 环境变量（需要在 Cloudflare Dashboard 中配置）
# VERCEL_DOMAIN - 你的 Vercel 部署域名
# CRON_SECRET - API 认证密钥（可选）
